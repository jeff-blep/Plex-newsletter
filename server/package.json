{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "node-cron": "^4.2.1",
    "nodemailer": "^7.0.5"
  }
}
{
  "scripts": {
    "api": "node server/index.mjs",
    "api:3101": "PORT=3101 node server/index.mjs",
    "api:nodemon": "nodemon --watch server --ext mjs,json --exec \"node server/index.mjs\""
  }
}
